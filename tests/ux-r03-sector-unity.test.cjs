const assert=require('node:assert/strict');const fs=require('node:fs');const data=fs.readFileSync('design-system-preview/component-data.js','utf8');const showcase=fs.readFileSync('experience-showcase/index.html','utf8');['قطع السيارات وخدماتها','المواد والتوريدات','العقارات'].forEach(sector=>assert.match(data,new RegExp(sector)));assert.match(showcase,/القطاعات فلاتر فقط/);assert.doesNotMatch(data,/sector-logo|قطاع.*شعار/i);console.log('ux-r03-sector-unity: PASS');