'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const path=require('node:path');
function load(){const p=path.resolve(__dirname,'../sw-vvip-static.js');delete require.cache[p];return require(p);}
function req(pathname){return{method:'GET',mode:'cors',destination:'worker',url:`https://app.example${pathname}`};}
test('F05 static delivery admits only F05 decoder WASM under workers/media',()=>{const{shouldHandleRequest}=load();assert.equal(shouldHandleRequest(req('/workers/media/f05-heif-decoder.v1.wasm'),'https://app.example','/'),true);assert.equal(shouldHandleRequest(req('/workers/random.wasm'),'https://app.example','/'),false);assert.equal(shouldHandleRequest(req('/workers/media/other.wasm'),'https://app.example','/'),false);assert.equal(shouldHandleRequest(req('/uploads/f05-heif-decoder.v1.wasm'),'https://app.example','/'),false);});
test('F05 change does not broaden cross-origin document or ordinary static rules',()=>{const{shouldHandleRequest}=load();assert.equal(shouldHandleRequest(req('/scripts/app.js'),'https://app.example','/'),true);assert.equal(shouldHandleRequest({...req('/workers/media/f05-heif-decoder.v1.wasm'),url:'https://evil.example/workers/media/f05-heif-decoder.v1.wasm'},'https://app.example','/'),false);assert.equal(shouldHandleRequest({...req('/workers/media/f05-heif-decoder.v1.wasm'),mode:'navigate',destination:'document'},'https://app.example','/'),false);});
