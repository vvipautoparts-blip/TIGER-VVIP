Expected RED conditions before implementation:

- current owner reference lacks TIGER NEXUS current authority language;
- machine authority lacks PULSE_VAULT and NOW/SMART/PRECISE markers;
- `scripts/nexus/living-sector-object.js` does not exist;
- `scripts/nexus/pulse-vault.js` does not exist;
- current composer still contains `بماذا تفكر؟`;
- current shell contains an inactive video placeholder;
- capability menu still mounts with `mountCapabilityMenu(null, document)`.

This file is evidence documentation only and must not replace executable CI evidence.
