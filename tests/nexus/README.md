# TIGER NEXUS contract tests

These tests are intentionally introduced before production implementation. They define the owner-approved NEXUS behavior and are expected to fail until the corresponding slice is implemented. No test fixture may preserve superseded current-tree product behavior as fallback or archive.
