# TP-01 TDD RED Trigger

This documentation-only commit exists to create a pull-request synchronize event while the TP-01 test contract is present and the production module is intentionally absent.

Expected result:

```text
TP01_TDD_PHASE=RED
EXPECTED_FAILURE=tigerpay-domain-contracts.js missing
PRODUCTION_MODULE_PRESENT=false
LIVE_PROVIDER_PRESENT=false
MONEY_MOVEMENT=false
```

This file is evidence only and may remain in the implementation branch as part of the TDD record.
