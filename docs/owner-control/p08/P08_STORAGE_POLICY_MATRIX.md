# P08 Storage Policy Matrix

The machine-readable storage contract is [P08_STORAGE_POLICY_MATRIX.json](P08_STORAGE_POLICY_MATRIX.json). All six buckets are private by default. Object access must be derived from the relevant profile, listing, ticket, case, or audit authorization, never from an unconditional policy.